import { OpenAPIV3 } from "openapi-types"

const property = (propertyObject: OpenAPIV3.SchemaObject, paramName?: string, required?: boolean): string => {
  propertyObject
  paramName
  required

  return ''
}

export { property }
